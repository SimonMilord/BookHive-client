
interface SearchResultsListProps {
  resultsArray: any[];
}

const SearchResultsList = ({ resultsArray }: SearchResultsListProps): JSX.Element => {
  return (
    <>

    </>
  );
};

export default SearchResultsList;